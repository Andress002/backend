/* import { Schema, Document, model } from 'mongoose';

interface IContratoAdmin extends Document {
  nombre: string;
}

const contratoSchema = new Schema<IContratoAdmin>(
  {
    nombre: { 
      type: String, 
      required: true 
    },
    
  },
  {
    timestamps: true,
  }
);

export default model<IContratoAdmin>('Contrato', contratoSchema);














 */